#include <stdio.h>

void square(int a)
{
    int b;
    b= a*a;
    printf("Area of square is : %d",b);
}

void main()
{
    int a;
    printf("Enter side of square : ");
    scanf("%d",&a);
    square(a);
    
}
