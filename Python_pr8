numbers = [5, 8, 3, 12, 7, 1, 9]

largest = numbers[0]
smallest = numbers[0]
for num in numbers:
    if (num > largest):
        largest = num
    if (num < smallest):
        smallest = num

print("Largest no. in the list:", largest)
print("Smallest no. in the list:", smallest)